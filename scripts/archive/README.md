# Historical scripts

This directory contains confirmed one-off migration scripts. It is excluded by `project.config.json` and
must not be imported, scanned, packaged, or called by formal workflows and Agents.

Each subdirectory records the original purpose and replacement. Historical scripts are retained during
stage one for traceability and may be deleted in a later compatibility-cleanup stage.
